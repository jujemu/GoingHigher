name: First Action

on:
  issues:
    types: [opend]    
  issue_comment:
    type: [created]    

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: tomsun28/issues-translate-action@v2.3
