name: First Action

on:
  issues:
    types: [opened]    
  issue_comment:
    type: [created]    

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: tomsun28/issues-translate-action@v2.3
